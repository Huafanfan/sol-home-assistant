# Sol Home Assistant：项目协作与文档优先约束

## 核心原则

本项目采用“文档驱动、规格先行”的开发方式。文档是产品意图、边界、隐私、成本、失败语义和验收标准的事实来源；代码是该事实来源的实现。

“文档比代码更详细”不表示逐行复述实现。每一条事实只保留一个权威位置，其他文档以链接引用，避免多份文档相互漂移。

用户的当次明确指令优先于本文件；涉及凭据、隐私、外部副作用或不可逆操作时，仍须遵守更严格的安全约束。

## 每个新 session 的启动流程

对 T1、T2、T3 变更，在修改任何实现、部署或运行配置前，必须：

1. 阅读 [`docs/START-HERE.md`](docs/START-HERE.md)，判定本次变更等级。
2. 阅读相关功能规格、架构文档和关联 ADR；涉及腾讯云或部署时，还须阅读相应 Provider/Deployment 文档。
3. 在工作说明中写出简短的“文档影响说明”：准备修改什么、变更等级、已阅读哪些文档、是否发现冲突。
4. 若不存在可接受的功能规格，先基于 [`docs/templates/feature-spec.md`](docs/templates/feature-spec.md) 建立规格；不得先写实现再倒推设计。

T0 变更也应检查不会改变可观察行为、数据语义、接口、成本、权限或运行边界；不能确定时一律上调为 T1。

## 变更等级与必需产物

| 等级 | 适用范围 | 实现前必须具备 |
| --- | --- | --- |
| T0 | 拼写、格式、测试文案、无可观察行为变化的局部重构 | 无新规格；变更说明须说明为何为 T0 |
| T1 | 新功能、用户可见行为、模块接口、状态机或测试契约变化 | 已接受的功能规格 |
| T2 | 架构、数据模型/保留、隐私、权限、供应商、成本、部署或网络边界变化 | 已接受的功能规格和 ADR |
| T3 | 不可逆操作或现实世界副作用，例如写入家庭设备、门锁、摄像头、外发消息或生产数据迁移 | T2 产物、明确的确认/回退设计，以及用户的明确授权 |

`accepted` 只表示规格已在既有用户目标和已接受决策的范围内可以实现；它不自动授予新的外部权限、费用承诺或高风险操作授权。T2/T3 出现新的取舍或范围扩展时，先请求用户确认。

## 功能规格生命周期

功能规格使用 `draft`、`accepted`、`implemented`、`verified`、`superseded` 状态：

- `draft`：正在探索，不能开始实现。
- `accepted`：设计边界完整、冲突已处理，可以开始实现。
- `implemented`：实现已落地，但验收证据尚未完整。
- `verified`：规格中的验收项已有真实测试、运行或人工验证证据。
- `superseded`：被新规格或 ADR 替代，必须链接替代项。

T1/T2 的规格（以及适用的 ADR）必须以仅文档提交先于第一笔实现提交，除非用户明确要求压缩提交历史；即使压缩，文档仍必须在第一笔实现编辑前达到 `accepted`。实现完成时，同步回填实现路径、测试/运行证据和实际偏差。没有证据不得标记 `verified`。

若实现与规格不一致，暂停将其视为完成：先说明差异，选择修正规格或实现；若影响 T2/T3 边界，新增或更新 ADR 后再继续。不得静默地把文档改写成已发生的代码行为。

## 文档职责

- `docs/START-HERE.md`：当前项目状态、阅读路径和文档导航。
- `docs/architecture.md`：稳定组件边界、数据流、状态机和安全模型。
- `docs/decisions/ADR-*.md`：跨模块、难逆转的决定及其后果。
- `docs/features/<feature>.md`：单个功能或新模块的可实现规格与验收证据。
- `docs/providers/`、`docs/deployment/`：外部服务和运行环境的事实、限制和操作边界。

不得在文档、日志、测试快照或提交信息中写入真实密钥、完整私人转录、原始音频或完整长期记忆。

## 实现与验收门槛

实现前，确认规格的目标、非目标、接口/数据、权限与隐私、取消/失败语义、成本影响和验收项均可回答。实现后，逐项复核本次 diff 是否改变了相关 ADR 或其他功能规格；若改变，必须同步更新。

最终交付说明必须区分：文档变更、实现变更、静态检查、测试、真实运行验证和尚未验证的假设。不得把“文档已写”或“配置可解析”描述为真实运行已验证。

## 协作与委托

若使用子代理或其他执行者，主代理始终负责需求、设计、架构、权衡、拆分、集成和最终验收。被委托的工作必须有明确文件边界和可独立验证的产物；执行者不得扩大范围、重定义已接受规格或绕过本文件的文档门槛。
